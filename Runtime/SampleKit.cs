using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace erlend
{ 
public class SampleKit : MonoBehaviour
{
        public float test = 10.0f;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
}